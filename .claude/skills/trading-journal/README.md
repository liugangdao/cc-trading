# Trading Journal Skill

将交易日志CLI系统集成到 Claude Code 中，通过斜杠命令快速操作。

## 安装

1. 确保已编译 `trading-cli` 可执行文件：
   ```bash
   go build -o trading-cli
   ```

2. Skill 已自动配置在 `.claude/skills/trading-journal/` 目录

## 可用命令

### /trade-open
记录新的开仓操作（交互式）

```
/trade-open
```

会启动交互式提示，引导你输入开仓信息。

### /trade-close
记录平仓操作（交互式）

```
/trade-close
```

显示未平仓位列表，选择并记录平仓信息。

### /trade-list
查询交易记录

```
# 查看所有交易
/trade-list

# 只看未平仓
/trade-list --status open

# 只看已平仓
/trade-list --status closed

# 按品种筛选
/trade-list --symbol BTC/USDT

# 按市场筛选
/trade-list --market crypto

# 按日期筛选
/trade-list --from 2025-01-01 --to 2025-01-31

# JSON格式
/trade-list --format json
```

### /trade-risk
分析当前持仓风险

```
/trade-risk
```

自动读取数据并分析：
- 总保证金和最大可能损失
- 每个仓位的风险回报比
- 仓位集中度
- 风险预警

### /trade-performance
分析交易表现

```
/trade-performance
```

自动分析历史数据：
- 整体胜率和盈亏比
- 品种和市场表现
- 止损止盈有效性
- 持仓时长分析
- 优化建议

## 使用示例

### 场景1：快速记录交易

```
你: /trade-open
Claude: [启动交互式开仓流程]

你: /trade-list --status open
Claude: [显示所有未平仓位]

你: /trade-close
Claude: [启动交互式平仓流程]
```

### 场景2：快速风险检查

```
你: /trade-risk
Claude: 正在分析风险...
[显示详细的风险分析报告]
```

### 场景3：定期表现回顾

```
你: /trade-performance
Claude: 正在分析交易表现...
[显示详细的表现分析和优化建议]
```

### 场景4：结合自然语言

```
你: /trade-list --status closed --format json

你: 根据上面的数据，哪个品种表现最好？

Claude: [分析JSON数据并回答]
```

## 优势

相比直接使用命令行：

✅ **更快捷** - 直接在对话中使用斜杠命令
✅ **更智能** - 结合 Claude 的分析能力
✅ **更灵活** - 可以随时提问和深入分析
✅ **更直观** - 在对话流中保持上下文

## 注意事项

- 确保 `trading-cli` 可执行文件在项目根目录
- Windows 用户需要使用 `trading-cli.exe`
- 交互式命令（open/close）需要在支持交互的终端环境中运行

## 自定义

可以在 `.claude/skills/trading-journal/skill.json` 中自定义：
- 添加更多命令
- 修改提示模板
- 调整默认参数

## 完整工作流示例

```bash
# 早上开仓
/trade-open
# [填写BTC多头信息]

# 查看当前持仓
/trade-list --status open

# 午间检查风险
/trade-risk

# 晚上平仓
/trade-close
# [选择BTC仓位平仓]

# 每周回顾
/trade-performance

# 深入分析特定品种
/trade-list --symbol BTC/USDT --format json
根据这些数据，我在BTC上的平均持仓时长是多少？是否应该调整策略？
```

这样你的交易日志系统就完全集成到 Claude Code 的工作流中了！🚀
